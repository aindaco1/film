# Testing

Run the baseline checks from the repo root:

```bash
npm run build
npm run test
npm run test:security
npm run test:migrations
npm run test:browser
npm run accept:screenplay -- /absolute/path/to/screenplay.fdx
npm run smoke:worker
npm run smoke:browser:worker
npm run smoke:providers:live
npm run smoke:auth:production
npm run smoke:local:worker
npm run report:production-traffic
npm run smoke
```

## Current Coverage

- `docs/USER_FLOWS.md` is the generated canonical inventory of 51 user flows. `scripts/user-flow-catalog.test.mjs` requires every flow to have concrete regression evidence and requires every application workspace to be represented. `npm run test:browser` drives all 17 workspaces on desktop and mobile, validates active navigation and headings, rejects visible enabled commands without a behavior contract, checks document overflow, and runs serious/critical axe checks.
- `scripts/screenplay-acceptance.test.mjs` verifies the reusable private screenplay acceptance chain. `npm run accept:screenplay -- /absolute/path/to/screenplay.fdx` parses a local Fountain/FDX source and exercises schedule, budget estimate, call sheet, sides, and report generation while printing aggregate source-free evidence only. The current Big Sword evidence is recorded in `docs/SCREENPLAY_ACCEPTANCE.md`.

- `packages/schema/test`: backup snapshot helpers, provider-shaped backup secret redaction, attachment manifest helpers, optional planning export snapshot inclusion, Dust Wave fixture coverage, formatting helpers, role/member helpers, task due-label normalization, document/asset/person/equipment/expense helpers with stable IDs, screenplay element inventory and occurrence-position projection with dismissed-record filtering plus source-text/excerpt omission, atomic bounded scene-element batch apply with added/reactivated/already-present counts and source-free summaries, bounded deterministic duplicate suggestions, explicit canonical merge coverage for occurrence deduplication/live-reference relinking/issued-call-sheet preservation, guarded cross-category moves with exact-name destination merging, script/stripboard scene-order projection with split-source deduplication and mismatched-source fallback, versioned stripboard transitions and reconciliation, bounded source-range scene split/move/reorder/merge and part-specific sides, main/second-unit legacy normalization and same-date cast collision checks, availability/date validation, deterministic conflicts and unit-aware DOOD with Travel/Hold annotation validation, counts, duplication, day removal, merge, and resolved-only revision carry-forward, bounded micro-budget assumptions, scenario metrics/comparison, schedule-budget integer-cent arithmetic and contingency bounds, schedule-day call-sheet snapshot generation/manifests/bounded edits/final locking plus explicit draft sync and manual-field preservation, daily report detail/scene-result/final transitions plus overnight timing summaries, expanded record-mutation field allowlists, film-profile mutation field allowlists, and operation sync validation including local task-status and task-completion operations.
- `apps/web/test`: static shell checks, project workspace section navigation including the mobile workspace rail, typed project onboarding, canonical snapshot client and fixture-removal/queued-edit reconciliation, stale-checked Markdown client behavior, metadata-only project search coverage that excludes Markdown bodies, local project/task/document/people/equipment/expense flows and Markdown handoff exports, Breakdown, Planning, and Backups workspaces, protected provider runtime readiness plus Google OAuth/Drive, Meta OAuth/Page/analytics, and Stripe clients, auth/invite/member/collaboration/mutation/permission controls, backup/restore commit chains including attachment bytes, bounded Notion ZIP/folder import plus the signed core-commit client with count-only summaries, attachment staging with size/type/signature/active-SVG rejection, attachment storage/export, and audit manifest behavior.
- `apps/worker/test`: auth/session/CORS/rate-limit behavior including Cloudflare's 60-second KV TTL minimum, stale KV role downgrade, and cross-workspace member/session rejection; bounded role/record-scoped canonical workspace snapshots; atomic stale-checked task transitions and Markdown body/audit writes; invite/member/collaboration/permission/mutation flows; protected provider runtime readiness, Stripe summaries, Google OAuth/Drive, Meta one-time OAuth/granted scopes/token encryption/Page mapping/partial analytics/disconnect plus signed deletion/deauthorization replay and redaction, encrypted SMS consent/evidence replay/revocation/manifests, outbound segment/quiet-hours/category/idempotency/provider-failure controls, terminal metadata retention, exact raw-body Telnyx signatures, signed-path identity extraction, transactional STOP suppression, signed delivery reconciliation, START non-reactivation, and route redaction/gates; bounded Notion preflight plus atomic create-only core/planning imports; atomic canonical operation replay; recoverable backup/attachment R2 uploads; backup/download planning; workspace/planning/attachment restore chains; and fail-closed authorization/storage/error paths.
- Live-auth coverage includes generic unknown-member responses with no token/hash disclosure, active-member-only Resend sends with non-PII persisted-link idempotency keys, delivery-attempt tag recovery after missing provider-ID persistence, fail-closed invite suppression/storage with atomic replacement, transactional acceptance and webhook materialization, atomic magic-link consumption/session creation with retry and post-commit KV-cache failure recovery, D1-authoritative logout despite KV delete failure, atomic member status/session-revocation/audit updates with retry, member/workspace-bound verification, truthful live verify/session/logout flags, unavailable-storage 503 responses for session reads, logout revocation, and mutation-rate KV, status-lookup failure denial, unscoped-session rejection, production demo-route suppression, and fail-closed D1/KV/session behavior. Attachment byte restore coverage includes browser ZIP entry size/hash verification, request metadata/body forwarding, Worker-side body hashing, create-only R2 writes, deterministic prepared reservations, atomic D1 intent/commit/audit finalization, corrupted-byte rejection, confirmed compensation cleanup, failed-compensation recovery, and idempotent retry without a second object write.
- `packages/backup/test`: encrypted legacy JSON and ZIP backup bundle round trips, private screenplay plus schedule/availability/budget/call-sheet/production-report graph restore without plaintext source leakage, ZIP manifest checks, central-directory tamper coverage for encrypted entries, ZIP64-size sentinels, and malformed central headers, separate encrypted document-body and attachment restore-policy payload coverage without plaintext body/path leakage, Dust Wave fixture smoke coverage, encrypted planning export payload inclusion without plaintext row leakage, passphrase validation, attachment metadata-only restore warning, attachment package restore-plan blockers, planning-aware restore preview warnings, per-kind and per-table coverage, bounded planning field-key samples, preview-only restore application plan rules, operation samples, table-level apply-plan counts, restore preview summaries, and per-record field conflict reporting.
- `packages/importers/test`: Notion manifest classification, authoritative candidate-cap application with project-database priority, bounded CSV character/row/column/cell parsing, malformed quoted-row and duplicate-header handling, normalized core-record provenance, Markdown/CSV record mapping, HTML export non-import so executable page content is ignored, Dust Wave-style relation-list mapping, production-planning database mapping counts, normalized planning rows with split project relation titles, attachment metadata mapping, screenplay file preflight for Fountain/Final Draft/Grainery files, deterministic Fountain and Final Draft scene/element parsing, stable graph IDs, XML entity rejection, unsafe path rejection, oversize rejection, and warning behavior.
- `packages/providers/test`: dry-run provider capability contracts, including Stripe summary-adapter live-read policy, Google Drive sync planning, and Resend, plus clone safety.
- `scripts/*.test.mjs`: deployment readiness script coverage for production-shaped config, invalid origin/adapter/mapping/rate-limit blockers, non-printing secret values, Worker `.dev.vars.example` key drift, non-secret Pool/Store companion Worker readiness checks, opt-in live Worker smoke coverage, production auth cleanup/retry behavior, and the explicitly gated production Notion operator path with source-body redaction and canonical verification.
- Production auth smoke tests cover the explicit send gate, delivered-message lookup, in-memory token use, session/logout/revocation flow, and output redaction for recipient, Resend key, message ID, link token, cookie, and CSRF metadata.
- Production traffic report tests cover Worker metric aggregation, active KV rate-limit window aggregation, and redaction of Cloudflare account, namespace, credential, and identity-hash values.
- Browser QA: `npm run test:browser` starts Vite and verifies the static operational workspace, desktop/mobile accessibility and responsive bounds, local screenplay parsing/review, filtered whole-revision element-list rendering, transient filtered scene-element copy plus idempotent paste and reload clearing, script/selected-stripboard scene order, occurrence navigation, deduplicated selected-scene reuse, deterministic local duplicate review plus explicit canonical merge, guarded category moves, and metadata-only Markdown/formula-safe UTF-8 CSV exports with ordered positions, versioned stripboard creation/assignment/reordering, transient multi-strip selection and atomic move round-trips with reload clearing, split-part movement and merge/main-or-second-unit selection/locking/reload, availability conflicts, unit-aware DOOD with editable non-work Travel/Hold state and reload/export persistence, micro-budget scenario comparison, editable schedule-budget arithmetic and metadata-only exports, schedule-linked call-sheet generation/detail and cast-call editing, explicit drift sync with manual-field preservation, finalization/reload/Markdown export, daily report details/scene results/finalization/reload plus Markdown and formula-safe scene CSV exports, local/canonical record flows, provider surfaces, encrypted backup/restore paths, Notion import, handoff exports, and multi-tab operation safety. Provider client tests cover authenticated SMS self-enrollment, the fixed disclosure acknowledgment, authenticated-member binding, and the redacted owner/producer consent manifest contract. Browser-against-Worker smoke covers signed record and profile mutation apply paths against real local/staging Worker configuration.
- `scripts/check-migrations.mjs`: applies every D1/SQLite migration chain to two fresh throwaway SQLite databases and checks integrity, foreign keys, expected tables, core owner metadata, and workspace-member profile columns, including all invite, collaboration, planning, backup/download, and restore proof/commit tables.
- Local Wrangler D1 state: from `apps/worker`, run `yes | npx wrangler d1 migrations apply DB --local`, then verify tables with `npx wrangler d1 execute DB --local --command "SELECT name FROM sqlite_master WHERE type = 'table' ORDER BY name;"`.
- Remote Wrangler D1 state: from `apps/worker`, run `yes | npx wrangler d1 migrations apply DB --remote`, then verify tables with `npx wrangler d1 execute DB --remote --command "SELECT name FROM sqlite_master WHERE type = 'table' ORDER BY name;"`.
- Worker deployment packaging: from `apps/worker`, run `npx wrangler deploy --dry-run` to verify the bundle and remote D1/KV/R2 bindings before publishing routes.
- Worker generated binding types: from `apps/worker`, run `npx wrangler types worker-configuration.d.ts --include-runtime false --check` after changing `wrangler.toml`.
- Live Worker smoke: start `npm run dev:worker` or choose a staging Worker origin, then run `FILM_WORKER_SMOKE_ORIGIN=http://127.0.0.1:8787 npm run smoke:worker`. Without an origin, the command skips; use `--require` or `FILM_WORKER_SMOKE_REQUIRED=1` when the gate must fail if no origin is configured.
- Browser against Worker smoke: start `npm run dev:worker` or choose a staging Worker origin, then run `FILM_WORKER_SMOKE_ORIGIN=http://127.0.0.1:8787 npm run smoke:browser:worker`. The script uses an existing app at `FILM_BROWSER_WORKER_SMOKE_APP_ORIGIN` or starts local Vite at `http://127.0.0.1:5173` with `VITE_WORKER_URL` pointed at the Worker origin. After signing in, it creates a local document, syncs it into canonical D1 rows, runs the protected core record mutation preflight/request/approval/diff/apply UI flow, runs the protected film-profile request/approval/diff/apply UI flow, downloads an encrypted backup, verifies either Worker R2 storage or Worker restore-point metadata, and previews the encrypted backup locally.
- Live provider adapter smoke: start `npm run dev:worker` or choose a staging Worker origin, then run `FILM_WORKER_SMOKE_ORIGIN=http://127.0.0.1:8787 npm run smoke:providers:live`. The command signs in, checks provider dry-run surfaces, Google Drive dry-run planning, Stripe summary readiness, and Resend invite-delivery readiness. It calls Pool/Store Stripe summary adapters only when `FILM_LIVE_PROVIDER_SMOKE_ALLOW_STRIPE=1`; it sends a live Resend invite only when `FILM_LIVE_PROVIDER_SMOKE_SEND_INVITE=1` and `FILM_LIVE_PROVIDER_SMOKE_INVITE_EMAIL` are set. Provider requests default to a 90s timeout for cold summary scans; use `FILM_LIVE_PROVIDER_SMOKE_TIMEOUT_MS` or `--timeout-ms` if a staging adapter needs a different budget. On July 9, 2026, a local smoke with Store on `127.0.0.1:8989`, Pool on `127.0.0.1:8990`, Film on `127.0.0.1:8787`, and a temporary deleted env file populated from companion `.dev.vars` passed with `FILM_LIVE_PROVIDER_SMOKE_ALLOW_STRIPE=1`; Resend live delivery remained blocked.
- Local Worker smoke suite: run `npm run smoke:local:worker` to apply local D1 migrations, start Wrangler at the repo's local CORS defaults (`http://127.0.0.1:8787` with the app smoke at `http://127.0.0.1:5173`), run the direct Worker, real-D1 member-status, collaboration, core restore proof-chain, and attachment restore proof-chain transactions, browser-against-Worker, and live-provider readiness smokes, then stop Wrangler. The isolated probes verify persistent disable-time session revocation; dry-run invite acceptance/revocation, comment intent, membership assign/revoke, stable permission upsert IDs, permission revoke, and owner transfer; guarded restore approval, commit-attempt, application-preflight, and planning preview; plus attachment package download plan, restore package preflight, verification, object planning, commit preflight, and matching audit evidence. Probe rows are removed afterward, and the attachment proof probe performs no byte write. Browser-against-Worker also exercises the atomic encrypted-backup download plan plus core and film-profile mutation request creation, resolution, and application. The suite forces local invite delivery to dry-run and applies a local-only bounded auth-request rate override so repeated runs do not inherit production delivery or limiter posture. Override `FILM_LOCAL_WORKER_SMOKE_WORKER_PORT` or `FILM_LOCAL_WORKER_SMOKE_APP_PORT` only when `ALLOWED_ORIGINS` is updated to match.
- Deployment readiness report: run `npm run check:deploy` to report production route/origin, invite delivery, Google OAuth, Meta disabled/live gate, and Stripe summary readiness without printing secret values. Use `npm run check:deploy -- --wrangler-secrets` to count Cloudflare secret names without reading values. Meta remains advisory while explicitly disabled and becomes strict when live mode is requested.
- Companion Worker readiness report: run `npm run check:companions` to verify the sibling Pool/Store repos expose tracked `/film/stripe-summary` route support and shared-secret binding names without printing local secret values. Use `npm run check:companions -- --strict` to fail on missing tracked adapter support.
- GitHub Actions: `.github/workflows/ci.yml` runs `npm ci`, installs Playwright Chromium, executes `npm run smoke`, reports `npm run check:deploy`, and packages the Worker with `npx wrangler deploy --dry-run` without requiring provider secrets. Companion checks stay local/operator-only because CI does not check out sibling Pool/Store repos.
- `scripts/check-secrets.mjs`: committed-secret scan over source/config/docs.

## Manual Smoke

1. Start the app with `npm run dev`.
2. Select each project from the sidebar and project table.
3. Toggle List and Board views.
4. Search for a project, person, task, document, equipment item, expense category, or call-sheet location. Open Projects and confirm `Export directory` downloads the filtered project directory without document bodies or raw import source paths.
5. Import a screenplay in Breakdown, open Schedule, create a stripboard, and date its shoot days. Select multiple whole-scene strips, move them to one day together, and confirm one atomic result; select mixed whole-scene and split-part strips in suitable data and repeat. Duplicate selection IDs must move once, an unknown strip or destination must leave every strip unchanged, selection must clear after a successful move/version change/reload, and locked schedules must disable selection. Assign/reorder scenes and split a multiline montage or long scene at a valid source line. Confirm `A`/`B` strips show bounded source ranges, move independently across days, persist after reload, and merge back into one unassigned source scene without changing the breakdown. Set one day to Second unit, duplicate and lock a version, and reload. Confirm unit and split controls lock with the schedule; unit identity appears in the stripboard export and flows into generated call sheets, sides, reports, and resource-use handoffs. Put one cast member on main- and second-unit days sharing a date and confirm one blocking cross-unit conflict; concurrent unit days must count consecutive-day streaks independently. Add cast/location availability and confirm unavailable resources block the exact dated day while missing confirmations remain warnings. Verify the unit-aware cast DOOD matrix, unlock the schedule, mark a non-work cast day Travel and another Hold, and confirm separate totals plus reload/export persistence. Work cells must remain derived and non-editable; locked schedules must disable all non-work selectors. Edit explicit micro-budget assumptions on an unlocked version, compare observed scenario metrics, and confirm breaches name the exact actual value and limit. Create a budget estimate, enter known crew/cast/location/equipment/move/meal rates and contingency, and verify the itemized integer-cent total. Confirm the UI labels it as an estimate with no inferred union rates or fringes. `Export stripboard` must declare `user_requested_schedule_metadata_export`, include split and cast-day annotation metadata plus the optional estimate, and omit source text. Production-clock stats, phase lanes, call-sheet metadata, and date-driven tasks remain read-only; `Export packet` still downloads the Markdown project packet.
6. Open Shots, filter to a screenplay scene, and add at least two shot records. Enter shot number, planned/ready/captured/omitted status, description, framing, angle, movement, lens, camera/support, frame rate, setup minutes/group, sound, lighting, general notes, and existing project-document references. Reorder a shot and confirm only that scene's shot ordinals change. Verify scene metadata, schedule days, and generated call-sheet use derive from existing collections and source text is not copied into the record. Save and reload. `Export list` must include the explicit shot decisions, resolved document names, and derived production use while omitting screenplay source/contact/provider/private data. `Export CSV` must use stable quoted columns, UTF-8, formula neutralization for values beginning with `=`, `+`, `-`, or `@`, and the same omissions. Verify desktop/mobile accessibility and no document overflow.
7. Open Call Sheets, choose an assigned schedule day, and generate a draft. Confirm ordered source scenes, split labels/ranges, unit identity, and reviewed cast requirements match that day; edit date/call/wrap, location, parking, hospital, weather, general/safety notes, performer, and a cast call. Change the source schedule and confirm the sheet warns without changing its scene/part snapshot. Click `Sync schedule` and verify the source date/unit/ordered strips/split ranges/cast requirements update while the sheet ID, title, manual logistics/notes, and matching performer/cast-call edits remain. Newly required cast should use the sheet's general call, removed requirements should disappear, and the warning/action should clear. Open Sides for a split call sheet and confirm only the synced source-line range appears. Finalize and reload, confirm every field persists read-only and later source drift has no sync action until the sheet is reopened. `Export call sheet` must include the generated scene/part/cast/logistics sections and project crew/gear/docs, omit screenplay source text and private/provider state, and include the selected project title and export policy note.
8. Open Sides and select the generated call sheet. Confirm the displayed scenes preserve its snapshot order, include only those screenplay source blocks and matching cast-call names, and do not create another persisted sides record. Change the source schedule or import a newer screenplay revision and confirm the pinned-source warning appears without silently moving the sides. `Source .md` must include the displayed screenplay text and explicit source-export policy. `Print HTML` must be standalone letter-sized HTML with escaped source, no scripts or external resources, and a restrictive content-security policy. Both downloads must omit contacts, raw attachment bytes, provider/private state, and raw import paths. Reload and confirm the same call-sheet-derived source view remains available; verify desktop/mobile accessibility and no document overflow.
9. Open Reports, create the selected generated call sheet's daily report, enter actual day timings/counts/notes, and mark planned scenes completed, partial, or held. Verify gross/meal/working durations and completion counts, including an overnight day. Finalize and reload, confirm the report is read-only, then reopen. Markdown export must contain the summary and operational notes; scene CSV must contain stable columns, quote every cell, neutralize values beginning with `=`, `+`, `-`, or `@`, and omit screenplay source text/private state.
10. Open Locations, add a scouting record from a reviewed screenplay location, and confirm scenes, schedule days, and availability derive from the existing production graph. Enter status, permit, address/contact, access, facilities, hospital, manual weather, safety, notes, and existing project-document references; save and reload. Confirm a non-confirmed location or final call sheet blocks logistics application, then confirm the location, reopen the sheet, apply it, and verify the call-sheet location/access/hospital/weather/safety snapshot. Export the local Markdown brief and verify it includes the explicitly entered contact/logistics fields while omitting screenplay source text, raw attachment bytes, provider/private state, and raw import paths. Confirm imported Notion locations remain a bounded source-review table.
11. Open Talent, add a record from a reviewed screenplay character, and confirm scenes, schedule days, and availability derive from the same production graph as Schedule and Locations. Enter casting/paperwork status, performer/direct/representative contacts, explicit rate basis/amount, deal, travel, dietary, accessibility, wardrobe, notes, and existing project-document references; save and reload. Confirm a non-cast/unlinked/unnamed record or final/non-matching call sheet blocks performer application, then cast the linked performer, reopen a matching sheet, apply it, and verify only the performer name snapshots into that character's cast call. Export the local Markdown brief and verify it includes explicitly entered private details plus the no-payroll/tax/union/legal/compliance limitation while omitting screenplay source text, raw attachment bytes, provider/private state, and raw import paths.
12. Open Planning, change the kind filter, and confirm only the bounded table view changes; no import, D1, or Worker write is triggered. Click `Export view` and confirm a Markdown file downloads without raw import source paths.
13. Confirm the Team section shows owner, producer, and director roles without raw emails. Click `Export team` and confirm a Markdown roster downloads with short email-hash references only.
14. Add a task, person, equipment item, and expense and confirm each appears at the top of the selected project's operational panels. In Tasks, confirm `Export tasks` downloads a Markdown task list. In People, confirm `Export crew` downloads a Markdown crew directory without contact fields. In Equipment, confirm `Export gear` downloads a Markdown gear pull. In Expenses, confirm the Budget Top Sheet updates and `Export budget` downloads a Markdown top sheet with the selected project title and export policy note.
15. Add a document and confirm a `.md` draft appears at the top of the selected project's docs.
16. Select a Markdown doc, enter body text, click `Save draft`, and confirm the topbar local operation count increases. With a hydrated canonical document and signed session, confirm the toast reports canonical workspace storage; with a stale version, confirm the local draft remains and the Worker reports `stale_document_version`. Click `Export draft` and confirm a Markdown file downloads with the local draft body and export policy note.
17. Click the project plus button, enter a real title and project type, and confirm the Film template is created locally and one `project.created` operation is queued.
18. Confirm the topbar local operation count increases.
19. Start the Worker with `npm run dev:worker`, request a magic link from the sidebar, verify the dry-run link, reload the same tab and confirm the session/workspace restore, then sign out. A separate new tab should require another magic link.
20. With a signed owner/producer session, create a Team invite, confirm a dry-run invite token appears, click `Review pending invites`, confirm the manifest shows only a short email hash and no token, click `Revoke` on a pending invite and confirm the count decreases, then create or use a remaining invite token in the sidebar invite-token form with a display name and confirm the accepted member appears in Team without a raw email address.
21. Disable a non-current Team member with `Update member status`, confirm the member remains visible as disabled and no longer appears in assignment/grant selects, then reactivate that member. Assign an active Team member to the selected project with a project role and optional department; click `Review project team`, click `Review project team history`, then click `Remove` on an assignment and confirm the manifest count decreases. Use the owner type and record selectors to review, transfer, inspect history, check update/delete access, request mutation review by selecting allowlisted field checkboxes, review mutation requests, resolve an approved/rejected request, preview an approved mutation diff with the typed per-field value controls, apply an approved request with the same update controls or an approved delete for project, task, document, person, equipment, and expense ownership, click `Review mutation audit` and confirm only metadata keys plus rollback guidance are shown, click `Request rollback` for an applied update and confirm it creates a new pending inverse mutation request with suggested values but no destructive write, then click `Plan delete recovery` for an applied delete and confirm it reports backup/recreate blockers without deleted row contents. Select profile field checkboxes, click `Request profile review`, review profile requests, resolve a request, preview the approved profile diff, and apply it only after the Worker returns the approved request ID. Create a project/task/document comment intent and confirm only a preview/hash is reported; click `Review comment intents` and confirm the protected manifest shows bounded preview/hash/author metadata only. Grant project, task, and document permissions with optional department/expiry and confirm the previews report Worker persistence. Click `Review project permissions`, `Review task permissions`, and `Review document permissions` to confirm protected manifests report current grants, click the matching permission history buttons to confirm bounded audit history, then click `Revoke` on a grant and confirm the manifest count decreases.
22. Click the topbar operation count and confirm queued operations mark as synced. With D1-backed auth, confirm task status/completion operations update canonical task state, the browser refreshes from the canonical snapshot, and the local audit entry includes Worker persistence.
23. Open the Activity tab, click `Export activity`, and confirm a Markdown local audit log downloads without Worker metadata values. Click `Worker audit`, and confirm the manifest lists recent Worker actions with metadata keys only.
24. Click `Backup now`, enter a passphrase with at least 12 characters, and confirm a `.filmbackup.zip` encrypted bundle downloads. With D1-backed Worker auth and `BACKUPS` available, confirm the toast reports R2 backup storage and the inspector shows Worker restore-point metadata plus latest-five retention; without R2, confirm the flow falls back to metadata-only Worker dry-run status.
25. Click `Stored backups` and confirm the inspector reports a bounded stored restore-point manifest count when signed in.
26. Select a stored restore point, click `Preview stored backup`, enter the passphrase, and confirm the Worker creates an expiring backup object download plan before bytes are fetched, then the backup is decrypted for preview only with no records overwritten.
27. Click `Preview encrypted backup`, choose a `.filmbackup.zip` or legacy `.filmbackup.json` file, enter the passphrase, and confirm the matching/new project plus per-record conflict summary appears without changing the selected project. Confirm the restore application plan reports preview-only blockers and bounded application operation samples. If the backup includes planning rows, confirm the preview reports those rows as preview-only, shows per-kind and per-table planning coverage, and shows a small planning-row sample with bounded field names.
28. If the backup includes planning rows and a signed owner/producer session is active, click `Check planning restore` and confirm the Worker reports create/idempotent/update/rejected planning-row counts, durable preview persistence, and no destructive writes.
29. With a signed owner/producer session, click `Check restore gate`, type `RESTORE <workspaceId>`, and confirm the Worker reports no destructive writes. If a stored R2 backup was created in this session, confirm pre-restore backup proof is verified and the gate reports `blocked until restore commit storage`; otherwise confirm it remains `blocked until pre restore backup`.
30. Click `Record approval`, type `RESTORE <workspaceId>`, and confirm the inspector reports `approved pending commit` with verified backup proof or `blocked` without proof. Confirm it still reports no destructive writes.
31. Click `Check commit storage`, type `RESTORE <workspaceId>`, and confirm the inspector reports `blocked until restore apply` with no destructive writes.
32. Click `Check application preflight`, type `RESTORE <workspaceId>`, and confirm the inspector reports `blocked until restore apply implementation`, rollback guidance blockers, and no destructive writes.
33. If the backup preview has workspace/project/task/document/person/equipment/expense creates or updates and verified pre-restore backup proof exists, confirm application preflight renders a snapshot-row review table with action, type, title, project, and detail columns before apply. Click `Apply snapshot records`, type `RESTORE <workspaceId>`, and confirm the inspector reports an application commit ID, `destructiveWrite: true`, and workspace/project/task/document/person/equipment/expense create/update/skip counts. Confirm attachment bytes are still outside the snapshot commit path.
34. If the backup includes planning rows, confirm `Check planning restore` renders a planning-row review table with action, kind/table, title, project, and field-key columns before apply. When the planning preview has no rejected records and verified pre-restore backup proof exists, click `Apply planning rows`, type `RESTORE <workspaceId>`, and confirm the inspector reports a planning commit ID, `destructiveWrite: true`, applied/skipped counts, and create/update counts.
35. If the backup preview includes attachment metadata, click `Check attachment package` and confirm the inspector reports byte restore blocked with no destructive writes. After a stored attachment package has been downloaded from Imports, click `Verify package manifest`, then `Plan attachment object restore`; confirm both report no destructive writes and the object plan shows destination, overwrite, byte-source, and manifest-hash proof statuses. Click `Check attachment commit preflight`, type `RESTORE <workspaceId>`, and confirm it records destination readiness or explicit blockers. When ready, click `Restore attachment bytes`, repeat the confirmation, and confirm each ZIP object is hash-verified locally and by the Worker before a no-overwrite R2/D1 commit is reported with `destructiveWrite: true`. Inject D1 finalization failure and confirm either R2/intent compensation completes or a matching prepared retry finalizes the existing object without a second upload.
36. Click `Restore` and confirm it reports a dry run.
37. Click `Import folder`, choose an extracted Notion export folder, and confirm Film creates its local review state. With a signed owner/producer session and matching selected canonical project, confirm the core summary reports committed/idempotent/update-preview/rejected counts and the workspace rehydrates from D1.
38. Click `Import ZIP`, choose a Notion `.zip`, and confirm Film records are reviewed, attachments show `R2 dry run`, and the import summary reports separate atomic core and planning commit/idempotency/update-preview/rejection counts plus Worker persistence modes. Confirm changed existing rows are previewed rather than overwritten and mismatched-project rows stay local with a warning.
39. Open `Breakdown`, click `Import`, and choose a `.fountain` or `.fdx` file. Confirm scenes, source excerpts, locations, cast cues, and any `[[category: value]]` tags appear; confirm or dismiss an element, refresh, and verify the review state persists. Before creating a schedule, confirm the `Schedule` scene-order segment is disabled. After reordering a matching stripboard, switch Breakdown to `Schedule`, verify day strips precede unassigned scenes and each split source appears once, then restore `Script` order. A cross-revision or missing schedule must fall back without hiding scenes. Search for a source phrase and an active element, confirm the matching scene list and match categories, clear the search, then reload after another search and confirm query text was not persisted. Add a missed element to the selected scene, confirm it starts reviewed, deduplicates when added again, persists through reload/encrypted backup, and appears in schedule/resource derivations without changing source text. Change the category filter and verify the whole-revision Element List updates from the same graph: dismissed items stay absent, the manual tag appears immediately, counts and first scene are correct, and there is no duplicate inventory state. Copy visible active elements under one category, select another scene in the same revision, paste, and confirm added/reactivated/already-present counts plus idempotent repeated paste; the copied selection must clear on reload and never appear in a Worker request. Expand an element's occurrence positions, verify every active scene/line/review state appears without excerpts, and jump to a source scene. Select a scene where the element is absent, apply the existing element, and confirm exactly one confirmed occurrence is added through the same graph; the action must disable once present. Add a similar same-category name and confirm the bounded local duplicate review appears without a network request. Open its review, choose each canonical direction in separate test data, and confirm the unkept row disappears, occurrence review state and distinct scene/line positions survive, exact collisions collapse, live availability/location/talent references use the kept ID, and existing generated call-sheet cast IDs/names remain unchanged. Move an unlinked element to another category and confirm its IDs, occurrences, and search result remain while the destination filter opens. Repeat with an exact normalized destination name and verify the option discloses the combine and the shared merge semantics apply. Confirm a location/talent/availability-linked element cannot move to an incompatible category and issued call-sheet cast snapshots do not block or change. Download Markdown and CSV handoffs; confirm both include ordered source-free positions while omitting source text/private state, CSV begins with a UTF-8 BOM, quotes stable columns, and neutralizes formula-leading cells. Export the breakdown and verify the JSON declares `user_requested_plaintext_export`. Import one revised draft while the prior revision is selected, confirm matching review decisions carry forward, select the comparison base, and verify unchanged/changed/added/removed counts plus per-scene matching reasons. Export the metadata-only revision report and confirm it contains no source text. Click `Carry planning forward`; confirm new draft schedule copies retain matched scene/day assignments, budget assumptions and availability copy once, matched shot/talent/location records relink, removed/unmatched records remain on the prior revision, and final call sheets/sides/reports still point to their original revision. Click the action again and confirm no duplicate schedule, budget, or availability copies appear. Confirm no screenplay request appears in the Worker network log. A `.gwx` file should create metadata only with an explicit warning.
40. With a signed session and local attachment blobs still staged, click `Store attachments` and confirm the import summary reports stored R2 attachments or rejected counts.
41. If stored R2 attachments exist, click `Export manifest` and confirm the import summary reports a bounded stored attachment export row count.
42. Click `Package attachments` and confirm the package plan reports object count, total bytes, an expiry time, and `zip download ready` when D1/R2 data is available and under the byte cap.
43. Click `Download package` and confirm the stored R2 attachment ZIP downloads only after the package plan is ready and the summary reports a verified SHA-256 prefix plus parsed manifest object count.
44. Click `Download latest attachment` and confirm the stored R2 object downloads only after the manifest is present and the summary reports a verified SHA-256 prefix.
45. Click each provider chip and confirm the protected summary appears. For Google, click `Check Google`; with the gate disabled, confirm only configuration names/blockers appear. With an approved staging OAuth client, connect in the same tab, confirm the callback query is removed, click `Read Drive`, enter an approved folder ID, and confirm at most 100 metadata rows render with pagination and no file content. Disconnect and confirm the connection becomes inactive. For Social, click `Check Meta`; while disabled, confirm only configuration-name blockers appear. In approved owned-account staging, connect in the same tab, confirm the callback query is removed, select a Page with `ANALYZE` and linked Instagram, read 30 days, confirm bounded calendar/insight rows contain no media URLs/tokens, then disconnect and confirm token/mapping deletion. For Stripe, confirm only mapped summary aggregates are returned.
46. With the Worker running, POST to `/api/restores/commit-dry-run`, `/api/restores/approval-dry-run`, `/api/restores/commit-storage-dry-run`, `/api/restores/application-dry-run`, `/api/restores/attachment-package-dry-run`, `/api/restores/attachment-package-verify-dry-run`, `/api/restores/attachment-objects-plan-dry-run`, `/api/restores/attachment-objects-commit-preflight`, and `/api/restores/planning-dry-run` with an authenticated owner/producer session and matching preview, package-plan, package-manifest, object-plan, confirmation, or planning-record metadata; confirm every dry-run/preflight response remains `destructiveWrite: false`.
47. POST to `/api/restores/application-commit` only with a matching approved approval, blocked commit attempt, blocked application preflight, verified pre-restore backup proof, exact confirmation, and bounded workspace/project/task/document/person/equipment/expense records; confirm it returns `destructiveWrite: true`, records `restore_application_commits`, and rejects stale create/update conflicts before writing rows.
48. POST to `/api/restores/planning-commit` only with a matching approved approval, blocked commit attempt, blocked application preflight, verified pre-restore backup proof, exact confirmation, at most 150 planning records, and a matching durable planning preview; confirm it returns `destructiveWrite: true`, records `restore_planning_commits`, and rejects stale planning-preview, duplicate-ID, invalid-timestamp, cross-workspace project, and create/update conflicts before writing rows. Inject a D1 batch failure and confirm planning rows, commit evidence, and commit audit evidence remain unchanged before retry.

## Planned Gates

- Run the manual Google connect/read/disconnect flow only after owner consent is approved; the automated probe intentionally stops before consent. Run the Meta signed deletion/deauthorization callback smoke after its real app secret is provisioned, and run connect/Page/read/disconnect only after its app, Login for Business, recoverable key, owned account, and review gates are approved. Telnyx SMS still requires provider resources and compliance approval.
