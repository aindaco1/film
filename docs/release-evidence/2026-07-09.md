# Film Release Evidence: 2026-07-09

## Deployment

- Static app: `https://film.dustwave.xyz`, HTTP 200, version `36e0a20d-acb6-4ec1-bc12-9d7da33f56ed`
- API Worker: `https://api.film.dustwave.xyz`, HTTP 200, version `30b2f8f7-0eb9-40a8-9d0b-61c430b0e7c9`
- Worker health: `production`, auth `live_member_only`, invite delivery `live`, Stripe `live_summary_only`
- Production bundle `assets/index-CU7idzki.js` contains the API origin and Google connection/Drive controls and does not contain provider credentials or the local Worker origin.

## Gates

- `npm run smoke`: passed 22 script tests, 133 web tests, 219 Worker tests, 10 backup tests, 16 importer tests, 7 provider tests, 19 schema tests, secret scan, two fresh migration chains, and desktop/mobile Playwright smoke.
- `npm run smoke:local:worker`: passed local migrations, direct Worker smoke, real-D1 member-status, collaboration, core restore proof-chain including planning preview, attachment restore proof-chain including package download planning, browser-against-Worker mutation/backup/download-plan/restore-preview smoke, and provider readiness smoke.
- `npm run check:deploy -- --wrangler-secrets`: counted five remote secret names without reading values and reports only the intentional Google client ID/client secret/live-mode blockers. Strict readiness remains blocked until Google consent and credentials are approved.
- `npm run check:companions -- --strict`: passed Pool and Store route/binding/summary-boundary checks.
- Worker and static app Wrangler packaging dry runs passed.
- `npm audit --omit=dev --audit-level=high` reported zero vulnerabilities.
- Focused Google UI screenshots at 1440x950 and 390x844 showed no horizontal overflow or console errors; the Connect action remained disabled under the production-shaped dry-run gate.
- Local D1 accepted the restore assertion success branch and returned `SQLITE_ERROR` integer overflow for the intentional rollback branch.
- Remote D1 reported no pending migrations through `0032_provider_connections.sql`.
- Remote D1 reported zero provider connections and zero active Google connections after deployment; no live Google API read was attempted.

## Production Probes

- `npm run smoke:auth:production -- --allow-send --require --check-runtime-readiness`: passed generic request, approved Resend delivery, truthful live verify/session/logout flags, member session, canonical workspace snapshot, four-live/three-blocked protected provider manifest, blocked/redacted Google connection status, logout, and revoked-session check without printing sensitive values.
- The first post-deploy magic-link request probe returned one fail-closed 503. An immediate non-member generic-response probe returned 202 without sending email, D1/KV checks remained healthy, the approved production auth/runtime smoke passed on retry, and Worker analytics reported no runtime invocation errors.
- Production auth smoke failures now identify only a redacted endpoint stage (Worker request/verification/session/runtime/logout or Resend list/detail) and never include recipient, provider message ID, token, cookie, or CSRF data.
- Production `GET /api/workspaces/demo` returned 404; signed-out `GET /api/auth/session` returned a D1-backed null session with `dryRun: false`.
- An unsigned protected rate-limited route reached the D1 auth boundary and returned 401, confirming the production KV limiter was available before authorization.
- Fault-injection coverage now proves current D1 roles override stale elevated KV roles, member/status/workspace mismatches fail closed, live rate-limit/session/logout failures return 503, permission-query failures cannot grant comment or mutation access, protected collaboration writes do not report dry-run success after D1 errors, and failed operation replay acknowledges zero queued IDs.
- Invite suppression and invite-row storage failures return 503 before Resend is called, while prior pending-invite revocation and replacement insertion now use one transactional D1 batch.
- Invite acceptance transactionally applies member role/status and consumes the pending token; verified webhook event, attempt status, and optional suppression rows also materialize in one retryable D1 batch.
- Live invite sends now use Resend idempotency keys and non-PII delivery-attempt tags; signed callbacks can recover and backfill a provider message ID after an outbound metadata-write failure.
- Notion ZIP import now validates UTF-8 paths, exact EOCD/central bounds, local/central path/flag/compression agreement, duplicate paths, single-disk/non-ZIP64 policy, compression ratio, aggregate and per-entry output bounds, and CRC-32. Folder and ZIP readers process only Worker-returned candidates sequentially; application enforces the same candidate cap, CSV parsing is character/row/column/cell bounded, and Worker preflight rejects missing-size or over-512 MiB manifests.
- Imported attachment staging now requires exact blob/manifest/document sizes, canonical MIME agreement, and format signatures before bytes are hashed or persisted. Invalid JSON, active SVG, renamed files, and malformed common media/document formats remain metadata-only and cannot enter R2 preparation.
- Workspace snapshot restore now validates canonical phase/status/priority/document/tone/relationship fields and commits at most 150 core rows, its durable destructive commit record, and audit evidence in one guarded D1 batch. Injected batch failure leaves all rows and evidence untouched; in-transaction existence/project assertions prevent stale create/update upsert races.
- Planning restore now rejects duplicate IDs, project-scoped roles, and invalid timestamps, then commits at most 150 non-idempotent planning rows, its durable destructive commit record, and audit evidence in one guarded D1 batch. Injected batch failure leaves planning rows and evidence untouched; in-transaction target/project assertions prevent stale create/update upsert races.
- Notion planning import now atomically batches create-only D1 rows with audit evidence, reports successful creates as destructive writes, rejects duplicate and cross-workspace deterministic IDs without row diffs, and leaves changed existing rows preview-only. Injected batch failure leaves all planning rows and audit evidence untouched.
- Attachment byte restore now reserves deterministic prepared intents before create-only R2 writes and atomically finalizes intent, destructive commit, and audit evidence in D1. Injected finalization failure proves confirmed compensation removes object/intent state, while failed R2 cleanup leaves a matching prepared reservation that recovers without a second upload.
- Encrypted backup and normal attachment uploads now use bounded body reads, create-only exact-state R2 writes, and atomic D1 metadata/audit finalization. Injected D1 failures return recoverable 503 states; retry finalizes the existing object without a second R2 put.
- Canonical offline operation replay now batches accepted workspace/project/task/document/person/equipment/expense creates, relationships, and operation-log evidence atomically with in-transaction conflict guards. Injected batch failure leaves every canonical table and operation log unchanged; cross-workspace project references and target IDs are rejected before writes.
- Approved core-record and film-profile mutation application now batches stale/request assertions, target writes, applied request plus rollback evidence, and deterministic audit evidence atomically. Injected D1 failures leave target, approved request, and audit state unchanged before successful retry.
- Workspace member disable/reactivate now batches an exact current-member assertion, status upsert, disable-time active-session revocation, and bounded audit evidence atomically. Injected D1 failure leaves member status, sessions, and audit state unchanged before successful retry; real local D1 confirms reactivation does not revive the revoked session.
- Project membership assign/revoke, record permission assign/revoke, and core owner transfer now batch current-state assertions, authorization writes, and bounded audit evidence atomically. Project assignment rejects IDs owned by another workspace, permission upserts preserve durable IDs, injected failures leave state and history unchanged, and real local D1 verifies the complete assign/update/transfer/revoke sequence.
- Metadata-only comment intents and core/film-profile mutation request creation and resolution now batch target/permission/request assertions, state, and bounded audit evidence atomically. Protected comment storage fails closed; injected failures leave no orphaned intent, request, status transition, or audit row before successful retry.
- Invite acceptance now asserts pending token and member identity state before atomically writing member role/status, one-time token consumption, and hash-only audit evidence; exact invite revocation and audit evidence are atomic too. Injected failures leave invites pending with no member or audit changes, and local D1 verifies accepted/revoked states with delivery forced to dry-run.
- Magic-link verification now atomically asserts and consumes the one-time link with D1 session creation. Injected D1 failure leaves the token reusable and creates no session; a post-commit KV role-cache failure preserves and returns the valid member-bound D1 session. Logout clears the cookie after authoritative D1 revocation even when KV cache deletion fails, and the stale cache entry cannot restore access.
- Live Resend magic-link delivery now uses the random persisted D1 link-record ID as a non-PII idempotency key; tests prove the header excludes email hashes and raw link tokens.
- Restore approval, commit-attempt, and application-preflight proof rows now reassert their upstream R2/approval/attempt state and persist bounded audit evidence in one D1 batch. Injected failures leave no proof or audit row before retry, and the local Wrangler probe verifies the complete chain. Stored-backup and audit-prefix queries now use wildcard-free exact-prefix predicates after real D1 exposed its 50-character LIKE-pattern ceiling against Film's 53-character R2 namespace.
- Attachment package preflight, package verification, object plan, and object commit preflight now reassert exact upstream proof state and persist each next proof with bounded audit evidence in one D1 batch. Injected failures leave no proof or audit row before retry; a dedicated real-D1 probe verifies all four stages without writing attachment bytes.
- Planning restore previews now persist their non-destructive proof row and bounded audit evidence in one D1 batch; injected failure leaves neither row before retry, and local D1 verifies the preview/audit pair.
- Encrypted backup-object and attachment-package download plans now persist hashed bearer-token rows and issuing audit evidence atomically before returning a token. Backup plan failure returns 503 with no plan/audit; attachment package plan failure returns a blocked no-token response, while successful retries produce one plan and one issuing audit. Local D1 exercises both plan types without logging token values or writing attachment bytes.
- Production project onboarding modal passed desktop/mobile viewport and serious/critical accessibility checks without submitting a project.
- Attachment commit CORS preflight returned 204 with the production origin and complete header allowlist.
- Unsigned attachment commit returned 403 `missing_csrf`.
- Production D1 had one workspace, one active owner, zero projects, tasks, documents, film profiles, project memberships, operation-log rows, record mutation requests, or film-profile mutation requests; zero active sessions or active workspace-less sessions; one retained revoked/expired unscoped session row; zero pending invites or invite suppressions; zero restore points or attachment intents; and zero snapshot, planning, or attachment restore commits after probes.
- No destructive production restore was performed.

## Traffic Posture

- `npm run report:production-traffic`: 593 Worker requests in 24 hours, zero runtime invocation errors, 26 subrequests, six active rate-limit windows, and maximum identity counts of one request in each observed auth/provider/mutation bucket.
- The default auth request limit remains 5 requests per 10 minutes. No `RATE_LIMIT_OVERRIDES` or challenge provider was added.
- Worker analytics errors are runtime invocation errors rather than HTTP status counts; KV evidence includes only unexpired windows.

## Provider Boundary

- Pool and Store summary adapter shared-secret values matched Film without printing values.
- The production live-provider smoke reached both summary-only adapters with explicit Stripe allow mode and returned sanitized aggregate data.
- Direct Stripe API reads remain disabled.
- Google authorization-code/PKCE, hashed one-time KV state, AES-GCM D1 token storage, refresh/revocation, connection status, and bounded Drive folder manifests are deployed behind `GOOGLE_OAUTH_MODE=dry_run`. The encryption key exists in Cloudflare and the login Keychain without being printed. Live Google remains blocked on approved client ID, client secret, consent/scope review, and the explicit live gate. Social and SMS remain provider-neutral dry-run contracts.
