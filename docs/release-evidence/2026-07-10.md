# Film Release Evidence: 2026-07-10

## Deployment

- Static app: `https://film.dustwave.xyz`, HTTP 200, version `4b437358-ce69-49c0-9082-0090e7aecc4b`.
- API Worker: `https://api.film.dustwave.xyz`, HTTP 200, version `c6fe801e-36d6-4d78-b1e3-4b2d9e00d6af`.
- Worker health reports `production`, auth `live_member_only`, invite delivery `live`, and Stripe `live_summary_only`.
- Dedicated privacy, terms, and data-deletion pages return their own HTML with HTTP 200.

## Big Sword

- The production auth/operator probe idempotently verified canonical project `project_big_sword` as `Big Sword`, type `Feature Film`, phase `development`.
- A direct bounded D1 read verified the matching film-profile row. No production record was changed by the verification.
- The sanitized Notion import snapshot contains 18 files: 10 Markdown and 8 CSV.
- The owner/producer-only production operator path committed the normalized core and planning records, then passed a full idempotent replay: 0 core committed/20 idempotent and 0 planning committed/4 idempotent.
- Independent bounded D1 reads verified one project/profile, six tasks, 11 documents, two project people, one equipment item, zero expenses, one imported location, and three imported production roles. One funding row was safely skipped instead of becoming an expense.
- Archive SHA-256: `31ffe80364bb7b54126c6b5915fcca9bc9f041973a9b8cfe943bd621c991f065`.
- The ignored local archive contains no contact PII, OAuth/provider values, expiring signed media URLs, or raw private attachment bytes.

## Provider Decisions

- ADR 0213 selects Telnyx for consented crew-only transactional SMS. Live sends remain blocked on account resources, 10DLC, an approved disclosure/retention policy, a send adapter, and an approved live smoke.
- The Telnyx metadata-only send planner rejects phone/message content, applies consent and bounded-volume policy, and always blocks live sending. Protected consent/revocation routes now use encrypted recipients, keyed lookup, evidence idempotency, atomic audit writes, opaque-ID revocation, and redacted manifests. The public messaging webhook is deployed but returns 404 while its explicit live gate is disabled; signed fixtures cover persistent event deduplication, transactional STOP, pending-attempt suppression, START non-reactivation, and response/audit redaction.
- ADR 0214 selects Meta Facebook Login for Business with a read-only Facebook/Instagram analytics boundary. Publishing remains in the Social application.
- Google OAuth now defaults to `drive.metadata.readonly`; Drive content and Calendar consent are separate future increments.
- Google Cloud project `Film` (`film-502013`, project number `1060718741054`) has the Drive API enabled, external testing audience, approved test user, production branding/policy URLs, and `dustwave.xyz` as an authorized domain.
- The `Film Web` OAuth client allows only `https://film.dustwave.xyz` as its JavaScript origin and `https://api.film.dustwave.xyz/api/providers/google/oauth/callback` as its redirect URI. Its replacement one-time secret was stored immediately in Wrangler and the login Keychain without being printed; the temporary credential files were removed.
- Public Google privacy disclosures document access, use, storage, sharing, security, retention, deletion, and Limited Use.

## Gates

- `npm run smoke`: passed 24 script tests, 138 web tests, 243 Worker tests, 10 backup tests, 17 importer tests, 11 provider tests, 19 schema tests, secret scan, two fresh migration chains, and desktop/mobile Playwright smoke.
- The script suite covers bounded retry cleanup after simulated post-session failure and a transient Google connection-readiness failure. Worker coverage includes independent Google OAuth/Drive, SMS encryption/HMAC identity, and Telnyx signature suites.
- `npm run smoke:auth:production -- --allow-send --require --check-runtime-readiness --create-project-title 'Big Sword' --project-type 'Feature Film' --project-id project_big_sword --apply-project`: passed approved message delivery, member session, canonical workspace snapshot, idempotent project verification, five live provider gates, live Google readiness, exact metadata-only authorization start, logout, and revoked-session enforcement. The authorization probe validates Google origin, production callback, PKCE S256, offline access, incremental/granular consent, and scope without navigating to consent or storing tokens.
- The same production probe with `--notion-source-dir <temporary-sanitized-export> --apply-notion-import` passed Worker preflight, deterministic core/planning replay, canonical ID verification, logout, and session revocation without printing source content or authentication values.
- `npm run check:companions -- --strict`: passed Pool and Store route, binding-name, source-boundary, and shared-secret-name checks without reading secret values.
- `npm run check:deploy -- --wrangler-secrets` and strict mode passed all production readiness checks, including Google client ID, client secret, callback, encryption key, and live mode without reading secret values.
- Remote D1 reports no pending migrations through `0033_sms_compliance_records.sql`.
- Migration 0033 created the encrypted-recipient, append-only consent, content-free delivery-attempt, and redacted Telnyx webhook-event tables. A bounded production read verified all four tables are empty.
- Independent SMS recipient encryption and HMAC keys were generated, backed up under Film-specific macOS Keychain service names, and stored as Wrangler secrets without printing values. The protected routes can write approved consent data, but no production recipient, consent event, delivery attempt, or Telnyx event has been created.
- Worker and static app Wrangler packaging dry runs passed.
- `npm audit --omit=dev --audit-level=high` reported zero vulnerabilities.
- Post-deploy health reported the expected production auth/invite/Stripe modes; the unsigned Telnyx messaging endpoint returned 404 because its live gate is intentionally unset. The owner production smoke then passed five-live/two-blocked runtime readiness, metadata-only Google authorization start, logout, and revoked-session enforcement.

## Traffic And Blockers

- The latest 24-hour production report counted 1,357 Worker requests, zero runtime invocation errors, 34 subrequests, one active rate-limit window, and a maximum observed auth request count of one.
- After the final Worker deploy, health and the unsigned Telnyx-route CSRF boundary passed. The owner smoke then encountered one Resend list failure, one fail-closed pre-verification 503, and one Resend delivery timeout; no token verification partially committed. One stale session from an earlier failed operator run was revoked, every unconsumed probe link was expired, and aggregate D1 verification ended with zero active sessions and zero active magic links.
- Google OAuth is deployed with `GOOGLE_OAUTH_MODE=live` and the metadata-only consent boundary. No active Google connection, access token, or refresh token exists; account consent remains an explicit owner action.
- Repeated operator probes reached the intended production magic-link limit of five requests per ten minutes. The probe did not bypass that gate; after the window reset, the final metadata-only authorization-start run passed.
- A production failure exposed a Cloudflare KV contract bug: rate-limit TTLs dropped below KV's 60-second minimum after the first request in a 60-second window. The Worker now floors physical KV TTLs at 60 while retaining logical reset timestamps; a regression test enforces the provider minimum.
- Final aggregate D1 verification reported zero active sessions, zero active magic links, zero active Google connections, zero SMS recipients, zero SMS consent events, zero SMS delivery attempts, and zero Telnyx webhook events. Count-only import evidence reported six core-import audits and four planning-import audits across the initial commit and deliberate idempotency probes.
- Meta Insights and Telnyx SMS remain explicit blocked contracts; no production OAuth tokens, phone numbers, message bodies, or provider credentials have been stored.
- Meta's blocked contract now has an implemented Worker-owned path: migration 0034 stores per-workspace encrypted user/Page tokens and explicit Page/linked-Instagram mappings; one-time KV state is session-bound; the callback verifies the granted read-only permission allowlist; Page selection requires `ANALYZE`; analytics are bounded to 31 days and redact tokens/media URLs/provider errors; disconnect attempts revocation and deletes local tokens/mappings. The static inspector exposes connection, Page selection, 30-day calendar/insight review, and disconnect without publishing controls.
- Focused Meta adapter/client tests and the complete Worker route lifecycle passed, including permission-expansion rejection, AAD-bound token encryption, callback replay rejection, partial endpoint failures, response/audit redaction, and local deletion. Migration 0034 passed two fresh SQLite replays and was applied to production D1.
- Production pins `META_GRAPH_API_VERSION=v23.0`, registers the exact Worker callback variable, and keeps `META_OAUTH_MODE=disabled`. Strict deployment readiness passes in that posture and will require every app/Login/key field if live mode is requested. The login Keychain was locked when the independent Meta key backup was attempted, so no Wrangler Meta token key was created; activation remains blocked until a recoverable copy can be stored first.
- The local Worker browser smoke now checks the disabled Meta inspector and captures focused desktop/mobile artifacts. A stale `Social dry run` assertion was corrected to the canonical `Meta Insights` label. Local D1 smoke helpers now bound each Wrangler command to 60 seconds and allow up to ten minutes for multi-command child probes, preventing an orphaned Wrangler process from hanging the suite indefinitely; member-status, collaboration, core restore, and attachment restore transaction probes all passed before the browser-tail correction.
- The disabled Meta implementation first deployed as Worker version `e6e03ada-cb8d-47d8-8abd-ee36dbe92435`. Migration 0035 and Worker version `2904e18f-60d1-4b3a-b183-d106337b0de0` add signed deauthorization and data-deletion callbacks, atomic local connection deletion, replay-stable confirmation records, and a redacted public status route. Production callback probes return 404 while the app secret is intentionally absent; `/health` remains 200, and production D1 reports zero Meta connections and zero deletion requests.
- The final responsive inspector assets deployed as web version `a280268b-6c65-45c5-81a6-7728b8580ceb`. Production serves JS `index-BLdxMHHu.js` and CSS `index-CYp_SaXZ.css`; the mobile provider screenshot confirms the status toast no longer covers provider controls or persistence text. A GET to the Meta OAuth callback returns `303` to `?meta=error&code=oauth_not_available`, confirming the production live gate remains closed.
- Post-deploy production auth smoke passed live member-only delivery, canonical workspace hydration, five-live/two-blocked provider readiness, metadata-only Google authorization start, logout, and revoked-session enforcement. Final production D1 verification reported zero active sessions, zero active magic links, zero Meta connections, and zero Meta deletion requests. The final full smoke passed 25 script tests, 139 web tests, 257 Worker tests, package suites, secret scan, two-pass migrations, browser overflow/accessibility checks, and the complete local Worker transaction/browser/provider suite.
- ADR 0222 adds the Telnyx outbound MVP path behind `SMS_MODE=disabled`: protected opaque-recipient batches, active category/member checks, GSM/UCS segment caps, configured quiet hours, safety-only reason-coded override, deterministic replay-safe attempt IDs, transient message content, redacted provider failures, and content-free attempt/audit records. Signed outbound delivery events now atomically advance the matching attempt; STOP remains authoritative.
- Migration 0036 adds retention indexes, and the Worker cron deletes only terminal attempt and Telnyx event metadata after an explicitly approved 30–730 day value. No production retention value is set while SMS is disabled, so the cron is a no-op. Production D1 reports both indexes present and zero SMS recipients, attempts, and Telnyx events.
- The disabled Telnyx adapter and daily cron deployed as Worker version `d1b4f192-7c9a-46f9-966d-25d889a8b75f`; the opaque-recipient composer deployed as web version `fa64390d-c55b-4e2c-82e7-9459bd3cec83` with JS `index-DscuFx8Y.js` and CSS `index-Dq4c_Qfa.css`. Desktop/mobile composer screenshots passed overflow and serious/critical accessibility checks, and the submit smoke confirmed transient content clears after render.
- Post-deploy production auth smoke additionally proved the authenticated SMS send route returns `telnyx_sms_send_disabled` without reflecting message or recipient metadata. Cleanup ended with zero active sessions and magic links. The final SMS release gate passed 26 script tests, 140 web tests, 265 Worker tests, package suites, secret scan, two-pass migrations, and browser QA.
- ADR 0223 corrects stale revenue readiness: production contains only `workspace_acme/project_big_sword`, while the tracked mappings referenced seed/fixture projects. Big Sword was absent from tracked Pool/Store catalogs, all 1,029 production companion KV key names, the Stripe CLI test catalog, and the live Stripe product catalog. No unrelated ref was substituted.
- Production now tracks `STRIPE_PROJECT_MAPPINGS={}` and `STRIPE_SUMMARY_MODE=disabled`; Pool/Store adapter URLs and shared-secret support remain deployed. Runtime readiness is expected to report two live providers (Resend and Google) and five blocked providers until exact Big Sword companion refs are created and reviewed.
- The corrected summary gate deployed as Worker version `8567ab9d-8311-4f8e-8aaf-9946e943c69a`. The first auth smoke reached the previous version during propagation and correctly failed its two-live assertion; after `/health` reported `stripeSummaryMode: dry_run`, the unchanged smoke passed two-live/five-blocked readiness, the disabled SMS boundary, Google metadata authorization start, logout, and revocation. Cleanup reported zero active sessions and magic links.
- Product owner confirmed Big Sword intentionally has no Pool campaign or Store products yet. Revenue mapping is deferred rather than an MVP blocker; the summary adapters, strict activation checks, companion contract tests, and opt-in live smoke remain ready for future canonical refs.
- The follow-up aggregate traffic report counted 1,161 Worker requests over 24 hours, zero runtime invocation errors, 34 subrequests, and zero active limiter windows. Default KV-backed limits remain unchanged and no Turnstile provider is justified by current evidence.
- The first explicit owner Google flow successfully reached the exact `drive.metadata.readonly` Google authorization request under the approved account, then stopped at Google's unverified external-testing warning. The warning remains an owner action; Film did not bypass it, exchange an authorization code, or store a Google token. Aggregate D1 verification reported one intentional owner Film session, zero unconsumed magic links, and zero active Google connections.
- That owner sign-in exposed an authenticated empty-backup-state crash in the static inspector. The inspector now renders `None` when no restore point exists; the web regression suite passed 140 tests, the production bundle has no console error after reload, and web version `a9aa1fb6-65d4-4469-bfdb-06c8c5374654` serves JS `index-BqTqqT15.js`.
- The same sign-in showed Resend click tracking rewriting the one-time URL. The verified `dustwave.xyz` Resend domain now reports both click and open tracking disabled; future auth and invite links stay direct while signed delivery/bounce/suppression handling remains enabled.
- Meta pre-activation hardening moved permission, identity, Page, analytics, and revocation access tokens to bearer headers and both token exchanges to form-encoded POST bodies. Regression tests reject token/app-secret query parameters; the complete Worker suite remains 265 passing tests with Meta still disabled. The hardened Worker deployed as version `c64eea13-bde8-45b6-8c23-b9291f47af80`.
- Migration validation now asserts the Meta/SMS tables, security-sensitive columns, and retention indexes introduced by migrations 0033-0036, and `.gitignore` explicitly allows the non-secret `apps/web/.env.production` API-origin file so clean production builds remain reproducible.
- Newly issued magic-link and invite URLs now carry one-time tokens in fragments rather than query parameters; the app removes the fragment before rendering, preserves legacy query compatibility for expiring links, automatically verifies magic links, and prefills invite acceptance. Browser smoke covers both deep-link paths. The final link-boundary release deployed Worker version `ef2fa678-9f5f-4cf9-b146-79e274b06271` and web version `785508fc-cf71-4bb1-9a13-96f7423b334a` with JS `index-SbjMgTrF.js`; focused gates passed 141 web tests, 265 Worker tests, both builds, and the full browser smoke.
- The unchanged production auth smoke passed twice after the link release; its final run fails if a newly delivered Film link exposes the token in the HTTP query and therefore proves the current Resend message uses the fragment contract. Strict deployment readiness, strict Pool/Store companion checks, and the production dependency audit all passed.
- Final `npm run smoke` passed 26 script tests, 141 web tests, 265 Worker tests, 57 package tests, every TypeScript/Vite build, committed-secret scanning, two fresh migration replays through 0036 with explicit Meta/SMS/index assertions, and the complete desktop/mobile browser smoke. Final aggregate D1 verification reported one intentionally preserved owner handoff session, zero active magic links, zero Google or Meta connections, zero SMS recipients, and zero SMS delivery attempts.
