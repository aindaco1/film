# Film Release Evidence: 2026-08-30

## Workflow and SMS consistency release

- Simplified navigation, inspector workflows, contextual record editing, and permission assignment while preserving the existing production feature set.
- Consolidated Worker request helpers, browser smoke helpers, local proof clients, script input parsing, and ZIP fixtures behind shared contracts.
- Centralized the Telnyx program identity, sender prefix, consent disclosure, disclosure version, categories, labels, and category validator in `@film/providers`.
- Added a carrier-surface regression that keeps the public SMS terms, in-app enrollment form, outbound sender, and 10DLC remediation packet aligned.
- Updated the 10DLC packet so `START` records a keyword response but directs a revoked crew member back through Film's explicit web enrollment flow.

## Verification

- `npm run smoke` passed 45 script tests, 170 web tests, 274 Worker tests, 82 package tests, every workspace build, the committed-secret scan, two fresh migration chains through migration 0036, and desktop/mobile browser accessibility and overflow coverage.
- `npm run smoke:local:worker` passed real local D1 migrations, Worker auth/provider smoke, member-status, collaboration, restore, attachment, browser-Worker, and provider-readiness flows.
- `npm run check:deploy:strict -- --wrangler-secrets` passed using remote secret names without reading values. Telnyx SMS, Meta, and Stripe summaries remain explicitly disabled behind their documented production gates.
- Worker and static-app Wrangler packaging dry runs passed before deployment.

## Deployment

- Source commit: `c4319ec`.
- API Worker version: `31d7b809-1495-487f-8d3e-1bff5ff8ba5b` at `https://api.film.dustwave.xyz`.
- Static app version: `af537f0f-ec12-4253-87dd-3ea775f4067b` at `https://film.dustwave.xyz`.
- Live health returned `ok: true` for `film-worker`.
- Live SMS terms identify `Film by Dust Wave`, state that Dust Wave operates Film, and publish the exact consent, STOP/HELP, privacy, and terms disclosures used by the campaign packet.
- The deployed `sms-consent-review.png` SHA-256 is `8e71ee38393941f677752e074a237ab5a1e817bb453f7e669afc7b32c7bafdc4`, exactly matching the tracked artifact.

## Remaining Telnyx gate

- Both `TELNYX_WEBHOOK_MODE` and `SMS_MODE` remain `disabled`.
- Production still needs the Telnyx API key, messaging-profile ID, corrected campaign ID, webhook public key, and 505-number-to-workspace mapping.
- After the corrected campaign enters review, install those values, run `npm run check:sms:preflight`, and use the protected Telnyx readiness route. Do not enable either live gate until campaign approval, signed webhook fixtures, and the owned-number smoke pass.
