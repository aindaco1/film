# Film Release Evidence: 2026-07-14

## Telnyx Readiness Release

- Added an owner/producer-only, read-only Telnyx provider check for the Film messaging profile, 10DLC campaign, aggregate carrier operation states, 505 messaging number, and phone-number campaign assignment.
- Responses contain configuration booleans, normalized statuses, aggregate carrier counts, bounded blockers, and gate state only. They exclude API keys, webhook keys, profile/campaign identifiers, phone numbers, carrier identifiers, and raw provider errors.
- The provider inspector now renders the pending-review state, profile/webhook readiness, aggregate carrier counts, number assignment, and explicit webhook/send gate state.
- Production stages `America/Denver` quiet hours from `22:00` to `07:00`, 90-day terminal delivery/webhook metadata retention, `TELNYX_WEBHOOK_MODE=disabled`, and `SMS_MODE=disabled`.
- Added `npm run check:sms:preflight` and ADR 0226. The preflight keeps both gates closed while requiring the API key, profile ID, campaign ID, public key, number mapping, encryption/HMAC keys, policy, D1, and retention cron.

## Verification

- Full `npm run smoke` passed 27 script tests, 145 web tests, 272 Worker tests, 57 package tests, every build, the committed-secret scan, two fresh migration chains through 0036, and desktop/mobile browser smoke.
- A follow-up browser smoke exercised the new Telnyx action and passed desktop/mobile accessibility and overflow checks. Rendered artifacts show `pending campaign review`, one approved carrier state, one review state, zero rejected states, and both activation gates closed.
- Worker and static-app Wrangler packaging dry runs passed. Strict production deployment readiness passed while SMS remains intentionally disabled.

## Deployment

- API Worker version: `74106489-7d9e-4f18-9cab-5fe4ed88eac5` at `https://api.film.dustwave.xyz`.
- Static app version: `605fbce3-36b0-4c61-b122-e42822aa7a4b` at `https://film.dustwave.xyz`, serving `index-BQqliKfZ.js`.
- Post-deploy health reports production member-only auth and live invite delivery. The unsigned Telnyx messaging webhook returns 404 while disabled, and the protected readiness route rejects unauthenticated requests before any provider call.
- Production still lacks the Telnyx API-key, profile-ID, campaign-ID, webhook-public-key, and 505-number mapping bindings. The values are held outside the repo and must be installed before Film can poll the pending campaign through the new route.
